# Phase 6 — cryptographic intelligence demonstrator\n\n- SourceScanner remains the only real scanner and now extracts safe X.509 PEM public metadata (RSA bits, EC curve, signature algorithm, expiry), key references, and manifest-only crypto dependency references. It never emits private key contents.\n- CryptoFinding normalization removes only exact hits; deterministic priority/rationale, conservative migration guidance, and CBOM-style metadata extend the existing pipeline without making discovery, risk, or CBOM depend on Obsidian text.\n- CLI passes only bounded context provenance (note count/character count) into the report. Binary, container, HSM, cloud, and dynamic/binary analysis remain future/mock architecture.\n- Validation: CLI 21 passed; backend 17 passed; Vite build passed.